/**
 * ===================================================================
 * CONFIGURACIÓN TEMPORAL - GEMINIS LABS
 * Configuraciones que se pueden cambiar fácilmente
 * ===================================================================
 */

/**
 * SELECTOR DE TEMAS TEMPORAL
 * Cambiar a false para ocultar el selector de temas
 */
export const SHOW_THEME_SELECTOR = true;

/**
 * CONFIGURACIÓN DEL SELECTOR DE TEMAS
 */
export const THEME_SELECTOR_CONFIG = {
	// Posición del selector
	position: {
		top: '100px',
		right: '20px'
	},

	// Mostrar en móviles
	showOnMobile: true,

	// Mostrar información adicional
	showDescription: true,

	// Mostrar preview de colores
	showColorPreview: true
};

/**
 * INSTRUCCIONES PARA DESACTIVAR:
 *
 * 1. Cambiar SHOW_THEME_SELECTOR a false
 * 2. Guardar el archivo
 * 3. Recargar la página
 *
 * El selector desaparecerá automáticamente
 */
